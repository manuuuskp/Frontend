// React context

// instead of passing props down to component by component this react context provides the way to pass down the props 
// this is the provider pattern.

// overusing the provider pattern can create performance related issues as the component which uses the context will rerender.