// facade pattern acts as an interface for a library or any complex classes.
// All the libraries will have an instance that act as a facade pattern.

// this pattern will provide the abstraction for a library as because the library provides the instance for it.