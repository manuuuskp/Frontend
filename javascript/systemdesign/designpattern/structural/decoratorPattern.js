// Decorator pattern provides some extra functions and properties to an object by wrapping around the object.
// The most common example of the decorator is the HOC of React, where one component wraps around other component to provide some added functionality or props.
